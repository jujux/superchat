<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/super_chat</item>
        <item name="colorPrimaryDark">@color/super_chat_dark</item>
        <item name="colorAccent">@color/super_chat_dark</item>
    </style>

    <style name="AppTheme.DialogueWhenLarge" parent="Theme.AppCompat.DialogWhenLarge">
        <item name="colorAccent">@color/super_chat_dark</item>
    </style>
</resources>